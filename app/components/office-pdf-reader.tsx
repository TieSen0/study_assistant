// Compatibility boundary: application code keeps this import stable while the
// reader implementation remains isolated under features/reader.
export { OfficePdfReader } from "../features/reader/reading-workspace";
